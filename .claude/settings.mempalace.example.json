{
  "_comment": "Opt-in mempalace integration. Copy the mcpServers and hooks blocks into .claude/settings.json (or ~/.claude/settings.json for user-wide) AFTER installing mempalace: `pip install mempalace && mempalace init .`. See https://mempalaceofficial.com/guide/hooks and https://mempalaceofficial.com/reference/mcp-tools for the current, canonical command strings.",

  "mcpServers": {
    "mempalace": {
      "command": "mempalace",
      "args": ["mcp"]
    }
  },

  "hooks": {
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "mempalace mine --mode convos --source-path \"$CLAUDE_PROJECT_DIR\" || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "mempalace mine --mode convos --source-path \"$CLAUDE_PROJECT_DIR\" || true"
          }
        ]
      }
    ]
  }
}
